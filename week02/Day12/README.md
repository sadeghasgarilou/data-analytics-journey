# Day 12

## What I learned
- methode of string like upper(), lower(), index('example'), startwith('test'), endwith('test'), split(), '*'.join(list)
- dictionary comprehension like list comprehension : {p:p[0] for p in planet}
- what is numpy
- import numpy by using of : import numpy as np
- different between python list and np array
- create np array with : np.array(list)
- inspect np array with array.shape and array.dtype
- math methode : np.sum(array), np.mean(array), np.min(array), np.max(array), np.std(array)
- indexing and slicing of np array : array[0], array[0, 1], array[:, 1]
- Operations in np arrays easily : array * 2, array + 4, array / 2
- create 2D array
- Axis : np.sum(array, axis=0), np.sum(array, axis=1)

## Exercises
- creating a np array and inspect it 
- create arrays using operation capabilty of np arrays
- create a np array and inspect it using indexing and slicing
- create a 2D np array and inspect it : I understand that when I want select second column I use this index array[:,1]

## Course
- Strings and Dictionaries From Kaggle python course
